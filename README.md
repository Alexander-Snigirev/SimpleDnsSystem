# SimpleDnsSystem

Программа реализует структуру данных Хэш-Таблица, используется метод цепочек, где вместо списков - AVL-деревья. Позволяет эффективно получать как доменное имя по ip, так и ip по доменному имени

1. Для запуска перейдите в папку ```src/``` и запустите файл ```main.py```